[main]
enabled=1
verbose=0
always_print_best_host = true
socket_timeout=3
#  Relative paths are relative to the cachedir (and so works for users as well
# as root).
hostfilepath=timedhosts.txt
maxhostfileage={{ yum_fm_maxhostfileage }}
maxthreads=15
include_only=.{{ yum_fm_pref_tld }}
prefer={{ yum_fm_pref_mirror }}
